<h1>Restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <h2><%= restaurant.name %></h2>
  <p><%= restaurant.category %></p>
  <p><%= link_to "Click here for more details", restaurant_path(restaurant) %></p>
<% end %>

<%= link_to "Add a restaurant", new_restaurant_path %>
